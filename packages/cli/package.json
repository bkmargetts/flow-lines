{
  "name": "@flow-lines/cli",
  "version": "0.1.0",
  "description": "CLI for generating flow line art",
  "type": "module",
  "bin": {
    "flow-lines": "./dist/cli.js"
  },
  "main": "./dist/cli.js",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src --ext .ts",
    "clean": "rm -rf dist",
    "start": "node dist/cli.js"
  },
  "keywords": [
    "generative-art",
    "cli",
    "flow-lines"
  ],
  "license": "MIT",
  "dependencies": {
    "@flow-lines/core": "workspace:*",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
